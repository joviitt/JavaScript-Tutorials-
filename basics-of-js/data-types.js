"use strict";  // this is not required, but its purpose is that it treats/processes all the JS CODE as the newer version of JS

// alert(3+3)  this is used for browser, but we are using nodejs

console.log(
    3+3) // js ignores whitespaces here but the readability is poor. READABILITY should be the first priority.

// ECMAscript(tc39.es) and mdn web docs(Mozilla) are some documentations we can refer for learning JS standards

let name = "Jovit"
let age = 21
let isSingle = true
let state = null
let city
/*
DATA TYPES:
    number,bigint(for bigger numbers,used while creating huge user-base apps)
    boolean
    string
    null=> standalone value
    undefined = unassigned value
    symbol = uniqueness

    object
*/

console.log(typeof state);

